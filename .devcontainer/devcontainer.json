{
  "name": "Flask + yt-dlp Dev",
  "image": "mcr.microsoft.com/devcontainers/python:3.11",
  "postCreateCommand": "apt-get update && apt-get install -y ffmpeg && pip install -r requirements.txt && pip install sqlite-utils",
  "forwardPorts": [5000],
  "customizations": {
    "vscode": {
      "extensions": ["ms-python.python"]
    }
  }
}
